# pip fallback only. Canonical source is pyproject.toml + uv.lock.
# On Windows, PyPI torch is CPU-only. For an NVIDIA GPU install a CUDA build, e.g.
# RTX 50-series (Blackwell), using the CUDA 13.0 wheels:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch>=2.0
torchvision>=0.15
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
scipy>=1.10
opencv-python-headless>=4.8
pillow>=10.0
pyyaml>=6.0
tqdm>=4.65
jupyter>=1.0
